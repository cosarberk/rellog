{
  "name": "rellog",
  "version": "1.0.0-beta.1",
  "description": "logger system for Relteco-Relnode",
  "main": "dist/index.js",
  "type": "commonjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "sudo rm -r dist && tsc",
    "dev": "nodemon ./build/index.js",
    "start:dev": "tsc && npm run dev",
    "test:npm": "npm run build && sudo npm unlink rellog && sudo npm link"
  },
  "keywords": [
    "rellog",
    "relnode"
  ],
  "author": "Berk Coşar <lookmainpoint@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.9.0",
    "nodemon": "^3.0.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "cli-table": "^0.3.11"
  }
}
